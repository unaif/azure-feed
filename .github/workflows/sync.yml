name: Sync Azure IP ranges

on:
  schedule:
    # Lunes 06:23 UTC — Microsoft publica actualizaciones los lunes
    - cron: '23 6 * * 1'
  workflow_dispatch: {}
  push:
    branches: [main]
    paths:
      - 'scripts/parse.py'
      - '.github/workflows/sync.yml'

permissions:
  contents: write

concurrency:
  group: sync-feed
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Run parser
        run: python3 scripts/parse.py

      - name: Commit if changed
        run: |
          git config user.name  "feed-bot"
          git config user.email "feed-bot@users.noreply.github.com"
          git add docs/
          if git diff --cached --quiet; then
            echo "Sin cambios"
          else
            git commit -m "chore: update Azure IP ranges $(date -u +%FT%TZ)"
            git push
          fi
